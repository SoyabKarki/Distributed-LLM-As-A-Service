apiVersion: v1
kind: Service 
metadata:
  name: backend
  namespace: llm-as-a-service
spec:
  selector:
    app: backend
  ports:
    - port: 8000  # The port other services use to reach this service
      targetPort: 8000  # The port on the backend container
  type: ClusterIP  # Internal service, only accessible within the cluster